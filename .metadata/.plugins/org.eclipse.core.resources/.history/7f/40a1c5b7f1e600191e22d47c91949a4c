import java.util.ArrayList;
import java.util.Random;

public class Snake extends Square {
	
	public Snake(int position, Game game, int transport){
		super(position, game);
		this.transport = transport;
	}
	
	int transport;
	
	Snake createRandomLadder(){		
		// We don't wanna have a random Number in the List as First or Last Square.
		int startPosition = 0;	
		while (startPosition < 7){			
			startPosition = new Random().nextInt(game.numberOfSquares() - 1);
		}
		int endPosition = 0;
		// We don't wanna have a random tansport below 2 or or at Last Square or higher
		while (endPosition < 2 || endPosition > game.numberOfSquares()-1){
			endPosition = game.numberOfSquares() - new Random().nextInt(5);
		}
		return (new Snake(startPosition, this.game, endPosition));
	}

	
	ArrayList<Snake> createLadderList(){
		int numberOfLadders = (int)((new Random().nextInt(game.numberOfSquares())+2)/1.5);
		int i = numberOfLadders;
		ArrayList<Snake> result = new ArrayList<Snake>();
		while(i > 0){
			result.add(createRandomLadder());
			i--;
		}
		return result;
	}
	
	
}
